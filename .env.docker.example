# DEMO / LOCAL ONLY — production secret kullanmayın.
# Kullanım: docker compose --env-file .env.docker.example up --build
# veya: copy .env.docker.example .env.docker  (gitignore)

POSTGRES_DB=cto_dashboard
POSTGRES_USER=postgres
POSTGRES_PASSWORD=postgres

JWT_SECRET=KolaysoftCtoDashboardDockerDemoJwtSecretKeyMustBeLongEnough
JWT_EXPIRATION_MS=3600000

SPRING_PROFILES_ACTIVE=dev
