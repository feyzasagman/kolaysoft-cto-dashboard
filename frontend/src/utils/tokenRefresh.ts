/**
 * Token yenileme placeholder.
 * Backend refresh endpoint eklendiğinde burada implement edilecek.
 */
export async function refreshAccessToken(): Promise<string | null> {
  // TODO(Day 13+): POST /api/v1/auth/refresh
  return null
}
